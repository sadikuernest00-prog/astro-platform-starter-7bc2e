---
const year = new Date().getFullYear();
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>AmicBridge | Lending & Investment Network</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      :root {
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          sans-serif;
        color-scheme: light;
      }

      body {
        margin: 0;
        padding: 0;
        background: #f3f4f6;
        color: #0f172a;
      }

      header.site-header {
        background: #0b1726;
        color: #e5e7eb;
        padding: 14px 0;
        margin-bottom: 24px;
        border-bottom: 1px solid #020617;
      }

      .header-inner {
        max-width: 960px;
        margin: 0 auto;
        padding: 0 18px;
        display: flex;
        align-items: center;
        justify-content: space-between;
      }

      .logo {
        font-weight: 600;
        font-size: 1.2rem;
        letter-spacing: 0.04em;
      }

      .logo span {
        color: #60a5fa;
      }

      .nav-links {
        display: flex;
        gap: 18px;
        font-size: 0.9rem;
      }

      .nav-links a {
        color: #e5e7eb;
        text-decoration: none;
      }

      .nav-links a:hover {
        text-decoration: underline;
      }

      main.page {
        max-width: 960px;
        margin: 0 auto;
        padding: 0 18px 40px;
      }

      .hero {
        display: grid;
        grid-template-columns: minmax(0, 3fr) minmax(0, 2.2fr);
        gap: 24px;
        align-items: center;
        margin-bottom: 24px;
      }

      .hero-left h1 {
        font-size: 2rem;
        margin: 0 0 8px;
        font-weight: 600;
      }

      .hero-left p {
        margin: 0 0 4px;
        font-size: 0.95rem;
        color: #4b5563;
      }

      .hero-left .small {
        font-size: 0.85rem;
        color: #6b7280;
      }

      .pill-row {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
        margin-top: 12px;
      }

      .pill {
        display: inline-block;
        font-size: 0.75rem;
        padding: 3px 10px;
        border-radius: 999px;
        border: 1px solid #d1d5db;
        background: #f9fafb;
        color: #374151;
      }

      .hero-right {
        background: #ffffff;
        border: 1px solid #e5e7eb;
        border-radius: 6px;
        padding: 12px 14px;
        font-size: 0.85rem;
        color: #374151;
      }

      .hero-right-title {
        font-weight: 600;
        margin-bottom: 6px;
        font-size: 0.9rem;
      }

      .hero-right ul {
        margin: 0;
        padding-left: 18px;
      }

      .hero-right li {
        margin-bottom: 4px;
      }

      .section-title {
        font-size: 1.3rem;
        margin: 4px 0 6px;
      }

      .section-sub {
        font-size: 0.85rem;
        color: #6b7280;
        margin: 0 0 6px;
      }

      .section-sub strong {
        color: #374151;
      }

      .card {
        background: #ffffff;
        border-radius: 6px;
        padding: 18px 16px 20px;
        border: 1px solid #e5e7eb;
        margin-top: 4px;
      }

      form {
        display: grid;
        gap: 14px;
        margin-top: 8px;
      }

      label {
        font-size: 0.85rem;
        font-weight: 500;
        display: block;
        margin-bottom: 4px;
      }

      input,
      textarea,
      select {
        font: inherit;
        padding: 8px 10px;
        border-radius: 4px;
        border: 1px solid #d1d5db;
        width: 100%;
        box-sizing: border-box;
        background: #f9fafb;
      }

      input:focus,
      textarea:focus,
      select:focus {
        outline: none;
        border-color: #2563eb;
        background: #ffffff;
      }

      textarea {
        resize: vertical;
        min-height: 80px;
      }

      .two-col {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
        gap: 14px;
      }

      .btn-row {
        display: flex;
        justify-content: flex-end;
        margin-top: 6px;
      }

      .btn {
        border-radius: 4px;
        padding: 9px 20px;
        font-size: 0.95rem;
        cursor: pointer;
        font-weight: 500;
        background: #0b1726;
        color: #ffffff;
        border: 1px solid #0b1726;
      }

      .btn:hover {
        background: #111827;
      }

      .checkbox-row {
        display: flex;
        align-items: flex-start;
        gap: 8px;
        font-size: 0.8rem;
        color: #374151;
      }

      .checkbox-row input {
        width: auto;
        margin-top: 2px;
      }

      .subtle {
        font-size: 0.8rem;
        color: #6b7280;
      }

      .notice {
        background: #f9fafb;
        border-radius: 6px;
        padding: 8px 10px;
        font-size: 0.78rem;
        color: #4b5563;
        border: 1px solid #e5e7eb;
        margin-top: 10px;
      }

      .about-section {
        margin-top: 24px;
        padding: 14px 16px;
        background: #ffffff;
        border-radius: 6px;
        border: 1px solid #e5e7eb;
      }

      .about-section h3 {
        font-size: 1rem;
        margin: 0 0 6px;
      }

      .about-section p {
        margin: 0;
        font-size: 0.85rem;
        color: #4b5563;
      }

      footer {
        margin-top: 26px;
        padding-top: 14px;
        border-top: 1px solid #e5e7eb;
        font-size: 0.75rem;
        color: #6b7280;
        text-align: center;
        line-height: 1.5;
      }

      @media (max-width: 768px) {
        .hero {
          grid-template-columns: minmax(0, 1fr);
        }
      }
    </style>
  </head>

  <body>
    <header class="site-header">
      <div class="header-inner">
        <div class="logo">
          Amic<span>Bridge</span>
        </div>
        <nav class="nav-links">
          <a href="#request">Request</a>
        </nav>
      </div>
    </header>

    <main class="page">
      <section class="hero">
        <div class="hero-left">
          <h1>Lending &amp; investment network</h1>
          <p>
            AmicBridge connects people and businesses that want to borrow,
            lend, invest and share profit.
          </p>
          <p class="small">Clear terms. Direct contact. Serious money only.</p>

          <div class="pill-row">
            <span class="pill">Borrowing</span>
            <span class="pill">Lending</span>
            <span class="pill">Investment</span>
            <span class="pill">Profit sharing</span>
          </div>
        </div>

        <div class="hero-right">
          <div class="hero-right-title">What you can request</div>
          <ul>
            <li>Borrow for business or private projects</li>
            <li>Lend capital with agreed return</li>
            <li>Invest in deals in the network</li>
            <li>Set up profit-sharing agreements</li>
            <li><strong>A written agreement is signed when a deal is made</strong></li>
          </ul>
        </div>
      </section>

      <section id="request">
        <h2 class="section-title">Send a request</h2>
        <p class="section-sub">
          Simple process:
          <strong>1)</strong> Send request · <strong>2)</strong> We review it ·
          <strong>3)</strong> We contact you if it fits the network.
        </p>

        <div class="card">
          <form
            action="https://formspree.io/f/xzznjkvd"
            method="POST"
            accept-charset="UTF-8"
          >
            <input type="hidden" name="_subject" value="New AmicBridge request" />
            <input type="hidden" name="_template" value="table" />

            <div class="two-col">
              <div>
                <label for="name">Full name</label>
                <input id="name" name="name" type="text" required />
              </div>
              <div>
                <label for="email">Email</label>
                <input id="email" name="email" type="email" required />
              </div>
            </div>

            <div class="two-col">
              <div>
                <label for="age">Age</label>
                <input id="age" name="age" type="number" min="18" max="120" required />
              </div>
              <div>
                <label for="work">Work / occupation</label>
                <input id="work" name="work" type="text" required />
              </div>
            </div>

            <div class="two-col">
              <div>
                <label for="income">Monthly income</label>
                <input id="income" name="income" type="number" required />
              </div>
              <div>
                <label for="currency">Main currency</label>
                <select id="currency" name="currency" required>
                  <option value="">Choose currency</option>
                  <option value="NOK">NOK</option>
                  <option value="EUR">EUR</option>
                  <option value="USD">USD</option>
                  <option value="SEK">SEK</option>
                  <option value="CRYPTO">Crypto</option>
                </select>
              </div>
            </div>

            <div class="two-col">
              <div>
                <label for="requestType">I want to</label>
                <select id="requestType" name="request_type" required>
                  <option value="">Select one</option>
                  <option value="BORROWING">Borrow</option>
                  <option value="LENDING">Lend</option>
                  <option value="INVESTMENT">Invest</option>
                  <option value="PROFIT_SHARING">Profit sharing</option>
                </select>
              </div>
              <div>
                <label for="amount">Amount</label>
                <input id="amount" name="amount" type="number" />
              </div>
            </div>

            <div>
              <label for="details">Short description</label>
              <textarea id="details" name="details"></textarea>
            </div>

            <div class="checkbox-row">
              <input id="confirm" name="confirm_information_correct" type="checkbox" required />
              <label for="confirm">
                I confirm my information is correct and AmicBridge may contact me.
              </label>
            </div>

            <div class="btn-row">
              <button class="btn" type="submit">Send request</button>
            </div>

            <p class="subtle">
              Your information is confidential and reviewed manually. Not shared for marketing.
            </p>
          </form>

          <div class="notice">
            AmicBridge is a private network, not a traditional bank.
            Agreements happen directly between parties.
            <strong>A written loan or investment agreement is signed when a deal is made.</strong>
          </div>
        </div>
      </section>

      <section class="about-section">
        <h3>About AmicBridge</h3>
        <p>
          AmicBridge is a private lending and investment network focused on
          structured agreements, clear terms and long-term cooperation.
        </p>
      </section>

      <footer>
        <div>AmicBridge &copy; {year}</div>
        <div>Private lending &amp; investment network</div>
        <div>CEO: Robert</div>
        <div>Contact: Robertamicolsen@gmail.com</div>
      </footer>
    </main>
  </body>
</html>
