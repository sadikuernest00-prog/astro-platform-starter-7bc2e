<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>AmicBridge – Structured Lending, Borrowing & Investing</title>
    <meta
      name="description"
      content="AmicBridge is a structured private network where people lend, borrow and invest with written agreements, clear expectations and shared profit."
    />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <style>
      :root {
        --bg: #f3f4f6;
        --white: #ffffff;
        --primary: #1d4ed8;
        --primary-dark: #1e3a8a;
        --border: #d1d5db;
        --text-main: #111827;
        --text-muted: #6b7280;
        --success-bg: #ecfdf3;
        --success-border: #bbf7d0;
        --success-text: #166534;
        --warn-bg: #fff7ed;
        --warn-border: #fed7aa;
        --warn-text: #9a3412;
      }

      * { box-sizing: border-box; }

      body {
        margin: 0;
        font-family: system-ui, -apple-system, Segoe UI, Arial, sans-serif;
        background: var(--bg);
        color: var(--text-main);
      }

      a { color: var(--primary); text-decoration: none; }
      a:hover { text-decoration: underline; }

      header {
        background: var(--white);
        border-bottom: 1px solid var(--border);
        padding: 14px 24px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 16px;
      }

      .brand-block { display: flex; flex-direction: column; gap: 4px; }

      .logo {
        font-weight: 800;
        font-size: 20px;
        letter-spacing: 0.08em;
        text-transform: uppercase;
      }

      .logo span { color: var(--primary-dark); }

      .top-meta {
        font-size: 11px;
        color: var(--text-muted);
      }

      nav {
        display: flex;
        gap: 18px;
        font-size: 14px;
        flex-wrap: wrap;
      }

      nav a { color: var(--text-main); font-weight: 500; }

      .container {
        max-width: 960px;
        margin: 0 auto;
        padding: 28px 16px 52px;
      }

      .hero {
        margin-top: 24px;
        display: grid;
        grid-template-columns: minmax(0, 3fr) minmax(0, 2fr);
        gap: 28px;
        align-items: center;
      }

      .trust-tag {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 4px 10px;
        border-radius: 999px;
        background: #e0f2fe;
        color: #0369a1;
        font-size: 11px;
        font-weight: 700;
        text-transform: uppercase;
        margin-bottom: 8px;
      }

      .hero h1 { margin: 0 0 8px; font-size: 32px; }
      .hero-subtitle { margin: 0 0 16px; font-size: 15px; color: var(--text-muted); max-width: 520px; }

      .hero-cta { display: flex; gap: 10px; flex-wrap: wrap; }

      .btn-primary {
        border-radius: 999px;
        padding: 9px 16px;
        border: 1px solid var(--primary-dark);
        background: var(--primary-dark);
        color: #fff;
        cursor: pointer;
        font-size: 14px;
        font-weight: 600;
      }

      .btn-secondary {
        border-radius: 999px;
        padding: 9px 16px;
        border: 1px solid var(--border);
        background: var(--white);
        color: var(--text-main);
        cursor: pointer;
        font-size: 14px;
        font-weight: 500;
      }

      .hero-card {
        background: var(--white);
        border-radius: 8px;
        border: 1px solid var(--border);
        padding: 14px 16px;
        font-size: 13px;
      }

      .hero-card h3 { margin: 0 0 8px; font-size: 15px; }

      .hero-pill-row { display: flex; flex-wrap: wrap; gap: 6px; }

      .hero-pill {
        padding: 4px 9px;
        border-radius: 999px;
        border: 1px solid var(--border);
        background: #f9fafb;
        font-size: 11px;
        color: var(--text-muted);
      }

      h2.section-title { font-size: 20px; margin-top: 40px; margin-bottom: 8px; }
      .section-sub { margin: 0 0 12px; font-size: 13px; color: var(--text-muted); }

      .grid-3 {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 16px;
      }

      .card {
        background: var(--white);
        border-radius: 8px;
        border: 1px solid var(--border);
        padding: 14px 16px;
        font-size: 14px;
      }

      .card h3 { margin: 0 0 6px; font-size: 15px; }
      .card p { margin: 0; color: var(--text-muted); }

      .list {
        margin: 6px 0 0;
        padding-left: 18px;
        color: var(--text-muted);
        font-size: 13px;
      }

      .list li { margin-bottom: 3px; }

      .two-col {
        display: grid;
        grid-template-columns: minmax(0, 3fr) minmax(0, 2fr);
        gap: 18px;
      }

      .badge-row { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 8px; }

      .badge {
        font-size: 11px;
        padding: 4px 10px;
        border-radius: 999px;
        background: var(--success-bg);
        border: 1px solid var(--success-border);
        color: var(--success-text);
      }

      .contact-card {
        background: var(--white);
        border-radius: 8px;
        border: 1px solid var(--border);
        padding: 14px 16px;
        font-size: 14px;
      }

      .steps {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 12px;
      }

      .step { font-size: 13px; color: var(--text-muted); }
      .step strong { display: block; color: var(--text-main); margin-bottom: 2px; }

      .offer-card {
        border: 1px solid var(--warn-border);
        background: var(--warn-bg);
      }

      .offer-grid {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 12px;
        margin-top: 10px;
      }

      .offer-box {
        background: var(--white);
        border: 1px solid var(--border);
        border-radius: 8px;
        padding: 10px 12px;
        font-size: 13px;
      }

      .offer-label {
        font-size: 11px;
        color: var(--text-muted);
      }

      .offer-value {
        font-weight: 800;
        font-size: 16px;
      }

      footer {
        font-size: 11px;
        color: var(--text-muted);
        border-top: 1px solid var(--border);
        padding: 12px 24px;
        background: var(--white);
        display: flex;
        justify-content: space-between;
        flex-wrap: wrap;
        gap: 8px;
      }

      @media (max-width: 900px) {
        header { flex-direction: column; align-items: flex-start; gap: 6px; }
        .hero { grid-template-columns: 1fr; }
        .grid-3 { grid-template-columns: 1fr; }
        .two-col { grid-template-columns: 1fr; }
        .steps { grid-template-columns: 1fr; }
        .offer-grid { grid-template-columns: 1fr; }
        .hero h1 { font-size: 26px; }
      }
    </style>
  </head>

  <body>
    <header>
      <div class="brand-block">
        <div class="logo">AMIC<span>BRIDGE</span></div>
        <div class="top-meta">
          Based in Norway · Private lending & investing network
        </div>
      </div>

      <nav>
        <a href="#offer">Current offer</a>
        <a href="#how">How it works</a>
        <a href="#who">Who it is for</a>
        <a href="#trust">Trust & agreements</a>
        <a href="#currency">Currency</a>
        <a href="#stablecoin">Stablecoin</a>
        <a href="#contact">Contact</a>
      </nav>
    </header>

    <div class="container">
      <section class="hero">
        <div>
          <div class="trust-tag"><span>✔</span> TRUST BEFORE MONEY</div>
          <h1>Structured lending, borrowing and investing between people.</h1>
          <p class="hero-subtitle">
            AmicBridge is a private network where people help each other with
            capital – always with written agreements, clear expectations and realistic profit sharing.
          </p>
          <div class="hero-cta">
            <a href="#contact"><button class="btn-primary">Contact CEO</button></a>
            <a href="#how"><button class="btn-secondary">Learn how it works</button></a>
          </div>
        </div>

        <aside class="hero-card">
          <h3>AmicBridge in one look</h3>
          <div class="hero-pill-row">
            <span class="hero-pill">Written agreements</span>
            <span class="hero-pill">Clear expectations</span>
            <span class="hero-pill">No hidden fees</span>
            <span class="hero-pill">Lend 45,000 → Receive 52,000 (4 weeks)</span>
          </div>
        </aside>
      </section>

      <section id="offer">
        <h2 class="section-title">Current offer</h2>
        <div class="card offer-card">
          <h3>Lending opportunity</h3>
          <p class="section-sub">
            Lend <strong>45,000</strong> and receive <strong>52,000</strong> in return after <strong>4 weeks</strong>.
            Terms, currency, and repayment schedule are defined in a written agreement before transfer.
          </p>

          <div class="offer-grid">
            <div class="offer-box">
              <div class="offer-label">You lend</div>
              <div class="offer-value">45,000</div>
            </div>
            <div class="offer-box">
              <div class="offer-label">You receive</div>
              <div class="offer-value">52,000</div>
            </div>
            <div class="offer-box">
              <div class="offer-label">Duration</div>
              <div class="offer-value">4 weeks</div>
            </div>
          </div>
        </div>
      </section>

      <footer>
        <span>© <span id="year"></span> AmicBridge · Trust Before Money</span>
        <span>Private financial network.</span>
      </footer>
    </div>

    <script>
      document.getElementById("year").textContent = new Date().getFullYear();
    </script>
  </body>
</html>
