
export default function AmicBridgeLanding() {
  return (
    <div className="min-h-screen bg-black text-white font-sans">
      {/* NAVBAR */}
      <header className="border-b border-zinc-800 bg-black/80 backdrop-blur sticky top-0 z-50">
        <div className="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
          <div>
            <h1 className="text-2xl font-bold tracking-tight">
              AmicBridge
            </h1>
          </div>

          <nav className="hidden md:flex items-center gap-8 text-sm text-zinc-300">
            <a href="#features" className="hover:text-white transition">
              Features
            </a>
            <a href="#security" className="hover:text-white transition">
              Security
            </a>
            <a href="#payouts" className="hover:text-white transition">
              Payouts
            </a>
            <a href="#faq" className="hover:text-white transition">
              FAQ
            </a>
          </nav>

          <button className="bg-white text-black px-5 py-2 rounded-2xl font-semibold hover:scale-105 transition">
            Connect Wallet
          </button>
        </div>
      </header>

      {/* HERO */}
      <section className="relative overflow-hidden">
        <div className="absolute inset-0 bg-gradient-to-br from-green-500/10 via-transparent to-blue-500/10" />

        <div className="max-w-7xl mx-auto px-6 py-28 relative z-10">
          <div className="max-w-3xl">
            <div className="inline-flex items-center gap-2 border border-zinc-700 rounded-full px-4 py-2 text-sm text-zinc-300 mb-6">
              Privacy • Freedom • Trust • Instant Payouts
            </div>

            <h2 className="text-6xl font-black leading-tight tracking-tight">
              Private. Fast.
              <br />
              Trusted Web3 Predictions.
            </h2>

            <p className="text-zinc-400 text-xl mt-6 leading-relaxed max-w-2xl">
              AmicBridge is a next-generation prediction platform built for crypto users who value privacy, instant payouts, and financial freedom.
            </p>

            <div className="flex flex-wrap gap-4 mt-10">
              <button className="bg-white text-black px-8 py-4 rounded-2xl font-bold hover:scale-105 transition">
                Start Predicting
              </button>

              <button className="border border-zinc-700 px-8 py-4 rounded-2xl font-bold hover:bg-zinc-900 transition">
                Learn More
              </button>
            </div>

            <div className="grid grid-cols-2 md:grid-cols-4 gap-6 mt-16">
              <div>
                <h3 className="text-3xl font-black">1s</h3>
                <p className="text-zinc-400 mt-2">Fast payouts</p>
              </div>

              <div>
                <h3 className="text-3xl font-black">24/7</h3>
                <p className="text-zinc-400 mt-2">Global access</p>
              </div>

              <div>
                <h3 className="text-3xl font-black">Multi</h3>
                <p className="text-zinc-400 mt-2">Crypto support</p>
              </div>

              <div>
                <h3 className="text-3xl font-black">100%</h3>
                <p className="text-zinc-400 mt-2">Wallet controlled</p>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* FEATURES */}
      <section id="features" className="py-24 border-t border-zinc-900">
        <div className="max-w-7xl mx-auto px-6">
          <div className="text-center max-w-3xl mx-auto mb-16">
            <h2 className="text-5xl font-black">
              Built Around Trust
            </h2>

            <p className="text-zinc-400 mt-6 text-lg">
              Every system inside AmicBridge is designed to create transparency, privacy, and confidence.
            </p>
          </div>

          <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
            <div className="bg-zinc-950 border border-zinc-800 rounded-3xl p-8">
              <div className="text-4xl mb-5">🔒</div>
              <h3 className="text-2xl font-bold mb-4">
                Privacy First
              </h3>
              <p className="text-zinc-400 leading-relaxed">
                Wallet-based access with minimized data collection and crypto-native infrastructure.
              </p>
            </div>

            <div className="bg-zinc-950 border border-zinc-800 rounded-3xl p-8">
              <div className="text-4xl mb-5">⚡</div>
              <h3 className="text-2xl font-bold mb-4">
                Instant Payouts
              </h3>
              <p className="text-zinc-400 leading-relaxed">
                Automated payout systems designed for fast withdrawals and transparent transactions.
              </p>
            </div>

            <div className="bg-zinc-950 border border-zinc-800 rounded-3xl p-8">
              <div className="text-4xl mb-5">🌍</div>
              <h3 className="text-2xl font-bold mb-4">
                Global Freedom
              </h3>
              <p className="text-zinc-400 leading-relaxed">
                Multi-chain access supporting decentralized participation from anywhere.
              </p>
            </div>

            <div className="bg-zinc-950 border border-zinc-800 rounded-3xl p-8">
              <div className="text-4xl mb-5">🛡️</div>
              <h3 className="text-2xl font-bold mb-4">
                Secure Systems
              </h3>
              <p className="text-zinc-400 leading-relaxed">
                Infrastructure protected with encrypted wallets, HTTPS, and anti-abuse systems.
              </p>
            </div>
          </div>
        </div>
      </section>

      {/* LIVE PAYOUTS */}
      <section id="payouts" className="py-24 border-t border-zinc-900 bg-zinc-950/30">
        <div className="max-w-7xl mx-auto px-6">
          <div className="flex items-center justify-between flex-wrap gap-4 mb-12">
            <div>
              <h2 className="text-5xl font-black">
                Latest Withdrawals
              </h2>
              <p className="text-zinc-400 mt-4 text-lg">
                Real-time payout transparency builds confidence.
              </p>
            </div>

            <div className="flex items-center gap-2 text-green-400">
              <div className="w-3 h-3 rounded-full bg-green-400 animate-pulse" />
              Live payouts active
            </div>
          </div>

          <div className="space-y-4">
            {[
              {
                wallet: '0xA91...2Df',
                amount: '120 USDC',
                status: 'Paid',
              },
              {
                wallet: '0x7C2...1Bc',
                amount: '0.4 ETH',
                status: 'Paid',
              },
              {
                wallet: '86dK...xmr',
                amount: '2.1 XMR',
                status: 'Paid',
              },
            ].map((item, index) => (
              <div
                key={index}
                className="bg-black border border-zinc-800 rounded-2xl p-6 flex items-center justify-between"
              >
                <div>
                  <p className="font-semibold text-lg">{item.wallet}</p>
                  <p className="text-zinc-500 mt-1">Withdrawal completed successfully</p>
                </div>

                <div className="text-right">
                  <p className="text-xl font-bold">{item.amount}</p>
                  <p className="text-green-400 mt-1">{item.status}</p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* SECURITY */}
      <section id="security" className="py-24 border-t border-zinc-900">
        <div className="max-w-7xl mx-auto px-6 grid lg:grid-cols-2 gap-16 items-center">
          <div>
            <h2 className="text-5xl font-black leading-tight">
              Security Designed For Web3
            </h2>

            <p className="text-zinc-400 text-lg mt-6 leading-relaxed">
              AmicBridge uses modern crypto infrastructure to protect user assets, reduce fraud, and create transparent transaction systems.
            </p>

            <div className="mt-10 space-y-6">
              {[
                'Encrypted wallet authentication',
                'Cold wallet reserve storage',
                'Automatic anti-fraud systems',
                'Cloudflare protection',
                'Transparent payout tracking',
                'Multi-chain transaction monitoring',
              ].map((feature, index) => (
                <div key={index} className="flex items-center gap-4">
                  <div className="w-4 h-4 rounded-full bg-green-400" />
                  <p className="text-lg">{feature}</p>
                </div>
              ))}
            </div>
          </div>

          <div className="bg-zinc-950 border border-zinc-800 rounded-[40px] p-10">
            <div className="space-y-8">
              <div>
                <div className="flex items-center justify-between mb-3">
                  <p className="text-zinc-400">System Security</p>
                  <p className="font-bold">99.9%</p>
                </div>

                <div className="h-3 rounded-full bg-zinc-800 overflow-hidden">
                  <div className="h-full w-[99%] bg-green-400 rounded-full" />
                </div>
              </div>

              <div>
                <div className="flex items-center justify-between mb-3">
                  <p className="text-zinc-400">Payout Speed</p>
                  <p className="font-bold">Instant</p>
                </div>

                <div className="h-3 rounded-full bg-zinc-800 overflow-hidden">
                  <div className="h-full w-[95%] bg-white rounded-full" />
                </div>
              </div>

              <div>
                <div className="flex items-center justify-between mb-3">
                  <p className="text-zinc-400">Privacy Protection</p>
                  <p className="font-bold">Advanced</p>
                </div>

                <div className="h-3 rounded-full bg-zinc-800 overflow-hidden">
                  <div className="h-full w-[97%] bg-blue-400 rounded-full" />
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* CRYPTO SUPPORT */}
      <section className="py-24 border-t border-zinc-900 bg-zinc-950/30">
        <div className="max-w-7xl mx-auto px-6 text-center">
          <h2 className="text-5xl font-black">
            Supported Assets
          </h2>

          <p className="text-zinc-400 mt-6 text-lg max-w-2xl mx-auto">
            Multi-chain infrastructure designed for fast and flexible Web3 participation.
          </p>

          <div className="grid grid-cols-2 md:grid-cols-4 gap-6 mt-14">
            {['ETH', 'USDC', 'BTC', 'XMR'].map((coin, index) => (
              <div
                key={index}
                className="bg-black border border-zinc-800 rounded-3xl p-10 hover:scale-105 transition"
              >
                <h3 className="text-4xl font-black">{coin}</h3>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* FAQ */}
      <section id="faq" className="py-24 border-t border-zinc-900">
        <div className="max-w-4xl mx-auto px-6">
          <div className="text-center mb-16">
            <h2 className="text-5xl font-black">
              Frequently Asked Questions
            </h2>
          </div>

          <div className="space-y-6">
            {[
              {
                q: 'How do payouts work?',
                a: 'Withdrawals are processed automatically using secure wallet infrastructure.',
              },
              {
                q: 'Do I need an account?',
                a: 'No traditional account is required. You can connect directly using your crypto wallet.',
              },
              {
                q: 'What cryptocurrencies are supported?',
                a: 'ETH, USDC, BTC, and Monero support are planned inside the ecosystem.',
              },
              {
                q: 'How is user privacy protected?',
                a: 'AmicBridge minimizes data collection and focuses on crypto-native infrastructure.',
              },
            ].map((item, index) => (
              <div
                key={index}
                className="bg-zinc-950 border border-zinc-800 rounded-3xl p-8"
              >
                <h3 className="text-2xl font-bold">
                  {item.q}
                </h3>

                <p className="text-zinc-400 mt-4 text-lg leading-relaxed">
                  {item.a}
                </p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* CTA */}
      <section className="py-28 border-t border-zinc-900">
        <div className="max-w-5xl mx-auto px-6 text-center">
          <h2 className="text-6xl font-black leading-tight">
            The Future Of
            <br />
            Trusted Web3 Predictions
          </h2>

          <p className="text-zinc-400 text-xl mt-8 max-w-3xl mx-auto leading-relaxed">
            Join the next generation of decentralized prediction infrastructure designed for speed, privacy, and freedom.
          </p>

          <div className="flex justify-center gap-4 mt-12 flex-wrap">
            <button className="bg-white text-black px-10 py-5 rounded-2xl text-lg font-bold hover:scale-105 transition">
              Launch App
            </button>

            <button className="border border-zinc-700 px-10 py-5 rounded-2xl text-lg font-bold hover:bg-zinc-900 transition">
              Read Documentation
            </button>
          </div>
        </div>
      </section>

      {/* FOOTER */}
      <footer className="border-t border-zinc-900 py-10">
        <div className="max-w-7xl mx-auto px-6 flex flex-col md:flex-row items-center justify-between gap-6">
          <div>
            <h3 className="text-2xl font-black">
              AmicBridge
            </h3>
            <p className="text-zinc-500 mt-2">
              Privacy-first Web3 prediction infrastructure.
            </p>
          </div>

          <div className="flex flex-wrap gap-6 text-zinc-400">
            <a href="#">Privacy Policy</a>
            <a href="#">Terms</a>
            <a href="#">Responsible Gambling</a>
            <a href="#">Contact</a>
          </div>
        </div>
      </footer>
    </div>
  )
}
