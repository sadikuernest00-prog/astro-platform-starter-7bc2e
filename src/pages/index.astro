---
const year = new Date().getFullYear();
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>AmicBridge – Structured Lending, Borrowing & Investing</title>
    <meta
      name="description"
      content="AmicBridge is a structured private network where people lend, borrow and invest with written agreements, clear expectations and shared profit."
    />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <style>
      :root {
        --bg: #f3f4f6;
        --white: #ffffff;
        --primary: #1d4ed8;
        --primary-dark: #1e3a8a;
        --border: #d1d5db;
        --text-main: #111827;
        --text-muted: #6b7280;
        --success-bg: #ecfdf3;
        --success-border: #bbf7d0;
        --success-text: #166534;
        --offer-bg: #e6f0ea;
      }

      * { box-sizing: border-box; }

      body {
        margin: 0;
        font-family: system-ui, -apple-system, Segoe UI, Arial, sans-serif;
        background: var(--bg);
        color: var(--text-main);
      }

      a { color: var(--primary); text-decoration: none; }
      a:hover { text-decoration: underline; }

      header {
        background: var(--white);
        border-bottom: 1px solid var(--border);
        padding: 14px 24px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 16px;
      }

      .brand-block { display: flex; flex-direction: column; gap: 4px; }

      .logo {
        font-weight: 800;
        font-size: 20px;
        letter-spacing: 0.08em;
        text-transform: uppercase;
      }

      .logo span { color: var(--primary-dark); }

      .top-meta {
        font-size: 11px;
        color: var(--text-muted);
      }

      nav {
        display: flex;
        gap: 18px;
        font-size: 14px;
        flex-wrap: wrap;
      }

      nav a { color: var(--text-main); font-weight: 500; }

      .container {
        max-width: 960px;
        margin: 0 auto;
        padding: 28px 16px 52px;
      }

      .section-title { font-size: 20px; margin-top: 40px; margin-bottom: 8px; }
      .section-sub { margin: 0 0 12px; font-size: 13px; color: var(--text-muted); }

      .card {
        background: var(--white);
        border-radius: 8px;
        border: 1px solid var(--border);
        padding: 14px 16px;
        font-size: 14px;
      }

      .list {
        margin: 6px 0 0;
        padding-left: 18px;
        color: var(--text-muted);
        font-size: 13px;
      }

      .two-col {
        display: grid;
        grid-template-columns: minmax(0, 3fr) minmax(0, 2fr);
        gap: 18px;
      }

      /* Offer */
      .offer-box {
        background: var(--offer-bg);
        border: 1px solid var(--border);
        border-radius: 8px;
        padding: 16px;
      }

      .offer-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        flex-wrap: wrap;
        gap: 8px;
        margin-bottom: 8px;
      }

      .offer-header h3 {
        margin: 0;
        font-size: 16px;
      }

      .offer-badge {
        background: var(--white);
        border: 1px solid var(--border);
        border-radius: 999px;
        padding: 4px 10px;
        font-size: 11px;
        font-weight: 700;
      }

      .offer-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 10px;
        margin-top: 10px;
      }

      .offer-item {
        background: var(--white);
        border: 1px solid var(--border);
        border-radius: 6px;
        padding: 10px;
        font-size: 13px;
      }

      .offer-item strong {
        display: block;
        font-size: 16px;
        margin-top: 4px;
      }

      footer {
        font-size: 11px;
        color: var(--text-muted);
        border-top: 1px solid var(--border);
        padding: 12px 24px;
        background: var(--white);
        display: flex;
        justify-content: space-between;
        flex-wrap: wrap;
        gap: 8px;
      }

      @media (max-width: 900px) {
        .two-col { grid-template-columns: 1fr; }
        .offer-grid { grid-template-columns: 1fr; }
      }
    </style>
  </head>

  <body>
    <header>
      <div class="brand-block">
        <div class="logo">AMIC<span>BRIDGE</span></div>
        <div class="top-meta">
          Based in Norway · Private lending & investing network
        </div>
      </div>

      <nav>
        <a href="#offer">Current offer</a>
        <a href="#contact">Contact</a>
      </nav>
    </header>

    <div class="container">

      <!-- CURRENT OFFER SECTION -->
      <section id="offer">
        <h2 class="section-title">Current Offer</h2>
        <p class="section-sub">
          Structured lending opportunity with written agreement and defined repayment.
        </p>

        <div class="offer-box">
          <div class="offer-header">
            <h3>Lend 45,000 → Receive 52,000</h3>
            <span class="offer-badge">Profit 7,000</span>
          </div>

          <p class="section-sub" style="margin-bottom:6px;">
            Repayment terms and timeline are clearly defined in the written agreement.
          </p>

          <div class="offer-grid">
            <div class="offer-item">
              Lend
              <strong>45,000</strong>
            </div>
            <div class="offer-item">
              Return
              <strong>52,000</strong>
            </div>
            <div class="offer-item">
              Profit
              <strong>7,000</strong>
            </div>
          </div>
        </div>
      </section>

      <section id="contact" style="margin-top:40px;">
        <h2 class="section-title">Contact</h2>

        <div class="two-col">
          <div class="card">
            <h3>Direct contact</h3>
            <p>CEO: <strong>Ernest</strong></p>

            <p style="font-size:14px;">
              Phone: 92928373<br />
              Email:
              <a href="mailto:amicbridgecontact@gmail.com">
                amicbridgecontact@gmail.com
              </a>
            </p>
          </div>

          <div class="card">
            <h3>When contacting</h3>
            <ul class="list">
              <li>Your role</li>
              <li>Amount</li>
              <li>Preferred currency</li>
              <li>Short description</li>
            </ul>
          </div>
        </div>
      </section>

    </div>

    <footer>
      <span>© {year} AmicBridge · Trust Before Money</span>
      <span>Private financial network.</span>
    </footer>
  </body>
</html>
